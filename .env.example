# Public Sesame website origin, used by the installed desktop app for the
# Sync status link.
# This is build-time configuration, never a runtime fallback for production.
VITE_SESAME_SITE_ORIGIN=http://localhost:4173
VITE_SESAME_CAPABILITY_PUBLIC_KEY=
